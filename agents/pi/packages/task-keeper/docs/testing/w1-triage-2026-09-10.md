# W1首轮分流（2026-09-10）

完成707项义务、436项矩阵及171条领域检查的下一动作分配。168条领域检查关联当前义务，G33的3条明确关联原P4延期义务。没有把P4再次加入本阶段。

W1.0盘点完成；不是全部oracle审核完成，W1自身35项缺口及详细验收仍保持开放。原140/313缺口、历史通过记录和冻结输入未改变。每条的下一动作见closure-obligations.csv/closure-matrices.csv，领域检查对应关系见closure-domain-checks.csv。

本次核对原WHEN/THEN及分层规则、最新报告的file/name/归属、实际入口与独立库的区别、36个配方与现有夹具。分流沿共享行为族复用，不为每个条目创建一套测试。

## 下一动作类别

| 类别 | 义务数 | 含义 |
|---|---:|---|
| production_unit_contract_review | 16 | 核对生产纯规则接口，不为计数新增无消费者库 |
| reuse_evidence_then_verify_oracles | 526 | 保留既有信用，核对缺少的变体/observer后复用 |
| boundary_or_implementation_first | 66 | 先处理F01–F04/适配边界 |
| missing_layer_or_observer | 93 | 优先扩展已有夹具的所缺层、断言或工件 |
| live_binding_required | 6 | 真实Qwen绑定先行；不可离线伪造 |

## 领域行为族

| 族 | 检查数 | 首轮下一动作 |
|---|---:|---|
| G01 包加载、初始化及配置隔离 | 4 | 复用隔离初始化/配置fixture；重复同步需对接已有Lua隔离同步证据，不扩展配置同步功能。 |
| G02 项目限制代数与禁止扩权 | 5 | 合并代数已有证据；F03暴露writer零值未执行，先修消费者，再在现有fixture补零值断言。 |
| G03 Qwen 多服务绑定与分类可替换 | 4 | 已有多服务分类和provenance fixture；检查实际HTTP观测与历史样本来源，不增provider特判。 |
| G04 能力证书、真实生效配置与启动屏障 | 4 | 按已认证foreground/fresh核对差异与拒绝；未支持模式不自动扩成新后端。 |
| G05 投递已接受但启动或执行中断 | 4 | 复用真实spawn失败/中断/正常native身份的现有observer。 |
| G06 关键事件去重、断序与补投递 | 4 | 复用event序列、reporter握手和通道丢失fixture；E须核对实际父输入。 |
| G07 五维结果、合法恢复及PARTIAL | 5 | Outcome和真实验证已有路径；检查required失败、恢复关系及最终父输入，不借库结果作E。 |
| G08 EvidencePacket容量、引用与不可信日志 | 5 | 引用/日志注入已有S与受管工具路径；补缺的实际入口及输入传播观测。 |
| G09 失败五层传播、低噪声状态及无UI | 5 | 状态和上下文走生产describe/contextEvidence；独立projectProgress库不能代替这些路径。 |
| G10 提议新鲜度、唯一动作及重复指纹 | 5 | F01：产品提议入口未接；先完成最小连接设计，禁止为独立ledger套E标签。 |
| G11 存储故障、迁移备份与回退 | 5 | 真实迁移/工件保存已有测试；U先确定生产规则接口，不能把真实IO改标U。 |
| G12 外部调用C0–C5崩溃与未知ACK | 4 | 复用原动作ID及native/workspace harness，补原C0–C5，不造另一执行器。 |
| G13 唯一owner、撤权及失联writer | 5 | 复用真实writer/PID/owner退出fixture；必须观察原写入者而非仅mock cancel。 |
| G14 静默构建、请求timeout与整体deadline | 4 | 复用finite timeout/deadline fixture；校核终止与unknown保留，不增加等待模式。 |
| G15 临时额度与永久、网络故障的分类边界 | 4 | 已有服务错误/压力分类样本；保持永久错误与配额语义分离。 |
| G16 冷却时间、时钟跳变与持久重启 | 5 | 复用实际Pi重启/时钟fixture；按原server floor和合并tick检查。 |
| G17 长上下文续跑、canary和完整流成功 | 6 | 复用SDK retry/完整流/失败流/canary/工具修改后恢复，补所需层级及工件。 |
| G18 工具命令共用状态与全部await撤权竞态 | 5 | 按现有入口与实际await边界核对；同步读取不能虚构await，不把终态恢复成活动。 |
| G19 十会话共享许可与独立任务进展 | 5 | 复用十父/独立池/park释放fixture；不重复创建十个实网任务。 |
| G20 硬条件先过滤与thinking语义 | 4 | 复用硬准入及实际route profile检查；只测试已选协议和拒绝范围。 |
| G21 恢复链阶段与事故备胎上限 | 5 | 复用真实备胎/持久stage fixture；原等待窗口和总预算不刷新。 |
| G22 账号遥测与网络路径失败 | 5 | direct与未认证代理拒绝已有路径；telemetry是同步文件，缺口不自动引入代理客户端。 |
| G23 每个真实请求的gate与累计预算 | 6 | 复用实际child fetch/SDK retry/重复结算；父helper拒绝边界保持。 |
| G24 依赖图与模型不能刷新任务预算 | 5 | 固定计划已有依赖规则；通用DAG仅内部fixture，不扩大模型授权入口。 |
| G25 资源原子预留、写冻结与队列公平 | 6 | 规范worktree排他已有实现；F02共享目录先接通，F03限值先落实；仅同父公平。 |
| G26 只读调查与持续工作区交付 | 5 | 复用真实创建失败/候选取消及受限reader；禁止从失败回退主树。 |
| G27 真实测试计数与验收输入变更 | 5 | 复用验收输入/授权版本变更fixture；未改范围不新增授权产品流。 |
| G28 有界recipe、环境错误及required预留 | 6 | 有限recipe已运行；selector库的隔离副作用测试不单独取得E；F04重规划另定最小契约。 |
| G29 独立审查、purpose与精确复用 | 5 | 复用真实审查、因果读取和同快照复用；不另建reviewer框架。 |
| G30 证据登记、来源漂移与诚实发布报告 | 6 | 复用报告器与版本锁；部分要求是报告/库契约，先审真实消费者。 |
| G31 验证器自身的负向控制与可重放序列 | 4 | 复用现有可失败的独立observer和replay；不能用高断言总数替代正确oracle。 |
| G32 Qwen Q3实际绑定、受控恢复与失败报告 | 5 | VAL-005/007 A/E/L需真实绑定；V报告规则可离线，不能给loopback实网信用。 |
| G33 P4在线Advisor延期义务的未来设计 | 3 | 明确P4在线延期，只保留未来设计，不新增本阶段运行义务。 |
| G34 根契约变更的明确用户授权 | 5 | 复用真实approve-checks/bindings及授权版本fixture；区分明确批准与普通resume，不清原消费。 |
| G35 主会话恢复与可选受管后端独立准入 | 4 | 复用主会话恢复与可选子后端独立启用/拒绝/重载fixture，不将child认证强加P1。 |
| G36 全部启动分母、分组留出和收益声明边界 | 4 | 已有全部启动项/holdout报告校验；不是实际策略效果比较。 |

## 已核准可开工项：W2/F03

原依据是配置仅收紧、固定单writer及任务4.2/8.4，对应CFG-004/CFG-008/T87。已有G02合并规则与固定workflow/真实Pi fixture可复用；只读探针已确认writersPerJob=0缺少实际消费者。

边界决议：writersPerJob是上限。0禁止受管writer，正值的实际容量为min(1,配置值)，保留固定单writer，不增加多writer模式。用户/项目继续取小值，直接adapter和child不能绕过0。该限制不夸大为禁止普通主会话文件工具或可信本地检查的全部写入。

最小变更：固定plan/受管提交拒绝fix writer；adapter派发与child grant复查落实零值；doctor显示配置值及实际容量。复用现有U、service-state、真实workflow及subagents fixture。只补F03反例、合法正例和边界绕过回归。

退出条件：0时受管入口/adapter/child零writer效果，1及更高上限仍单writer；项目收紧不被提高，inspect保持独立准入。F01/F02/F04继续各自最小设计，不因F03完成而关闭。
