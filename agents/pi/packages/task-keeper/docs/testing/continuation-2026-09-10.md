# 2026-09-10 收口执行记录

当前完整运行：`2026-09-09T17-56-30-491Z`；765 passed，0 failed/cancelled/skipped，16,170 assertions。源码/测试摘要：`1d01d80838f85f49eac4cc4baba46282cfaf67281edf05ce1179e12fe327a936`，刷新记录时精确匹配。类型检查、静态计划检查（126 scenarios + 102 faults）及OpenSpec严格校验通过。

## 本批完成

- W1.0完成707项义务、436项矩阵和171条领域检查的分流；其中3条G33领域检查保留在原P4延期范围。详细oracle审计仍开放。
- F01：`decisions/propose`接通真实命令、模型工具与同一Scheduler/Store；登记、资源等待、最终契约复查及拒绝没有第二个dispatcher。
- F02：可信check绑定可声明共享可变目录；canonical别名共用资源，writer/verify原子争用，实际目录漂移阻塞。
- F03：writersPerJob=0在计划/适配器/child拒绝受管writer；正值保持单writer，doctor显示有效容量。
- F04：可选的诊断后修复实际改变固定图并消耗原重规划上限；默认关闭，0禁用，不刷新语义/step/request计数。

具体实现与验收边界：[F01](w2-f01-design-2026-09-10.md)、[F02](w2-f02-design-2026-09-10.md)、[F03](w2-f03-2026-09-10.md)、[F04](w2-f04-design-2026-09-10.md)。W2的四项实现差异已处理，但W2所属的每条完整义务并未因此自动关闭。

新增19条运行回归，均针对F01–F04的实际新增调用链或已定位缺陷：F03 5条、F02 4条、F01 6条、F04 4条。其余修改复用原有测试。没有增加707项义务或436项矩阵分母，未从测试总数推算规格完成度。

本次新增最低证据：EVD-007:E、EVD-008:E、EVD-009:E、SCH-009:E、TK05:E。剩余135项；细分矩阵仍123/436、缺313。两个视图关联同一实现，不能相加。TK06:E的反向冻结/竞争没有被借用为已覆盖，W3–W5仍需按原observer/层级核对。

## 失败与修正（均保留）

- F03初始U反例：0时原计划未抛WRITERS_DISABLED；修复后通过。
- 完整运行`2026-09-09T16-54-37-237Z`：749/751，两个失败。十Pi会话多了一次receiver之前的Connection error，导致额外transport占用；旧日志不能确认底层原因。测试显式关闭首轮HTTP连接，固定其quota/owner-loss输入，不声称修复了新的运行时网络故障。另一个取消测试读取CANCELLED后立即断言job lease已释放，现改为观察两个已存在事实都完成。
- 完整运行`2026-09-09T17-07-57-455Z`：754/755。旧最小job夹具缺workflow；补齐后模型绑定检查通过。
- 完整运行`2026-09-09T17-13-01-598Z`：755/755。随后审核撤销过宽的TK06 E归属，未改写该历史报告。
- F01首轮真实提议触发NESTED_TRANSACTION，已改为原准入事务内关联；模型工具测试曾因JSON编码观察器误报等待，改为解析实际工具结果。相关定向失败工件保留。
- 完整运行`2026-09-09T17-32-11-764Z`：原本地验证预算测试在全套并发下触及其1秒fixture timeout；该场景不测试超时，改为5秒，保留原预算断言。报告器同时拒绝proposal-stale借用未声明的EVD-009归属；现将非法/重复断言限定在其对应场景，未绕过报告检查。
- 独立F02负向控制只移除测试副本的writer共享资源，实际请求时claim=0（期望1），按预期失败；控制源码及manifest保留。正常来源的765项完整运行是本批通过依据。

本批未运行真实provider/Qwen API、没有写真实Pi/Claude home、没有同步/部署/提交/归档。旧Claude配置事件的恢复缺口不在本批中假定已解决。

## 后续固定入口

依[收口计划](../../../../../openspec/changes/add-pi-task-keeper/closure-plan.md)继续W1详细审计、W2剩余证据及W3–W7。Qwen实网的profile引用与总请求上限仍待输入；不读取密钥来凑认证。`releaseReady=false`，历史任务仍71/76，7.4/10.2/11.6/12.1/13.5保持开放。

## W2剩余义务复核后的检查点

当前完整运行`2026-09-09T18-47-02-150Z`：767通过、0失败/取消/跳过，16,585断言；源码/测试摘要`5aa4b7a1e05a048298729fc96feb811e8f26adaee27da336258a714f3df1b97f`。最低缺口125；矩阵缺口313。原W2的15项最低缺口现均有证据，详细变体审核不自动关闭。

本阶段在已有真实proposal-valid/stale场景中补齐T59/T60/T62–T65/T70/RTB-019/T85的具体输入、断言与下一次父模型输入；只新增目录身份U和过期pending恢复S两条回归，没有复制整套夹具。相对初始746项，累计新增21条运行测试，验收分母仍为707/436。

新增发现并修复：CLI非法JSON及越权提议现在留下受控拒绝索引；它们不改变语义revision，也不造成默认advance。过期pending若不移除会永久阻挡后续resume，原反例失败后已修复为保留拒绝记录并退出pending。候选变化后，未成功运行的baseline也会重新准入，真实Pi最终能完成原目标，而非只停在拒绝状态。

T60使用同state root真实资源进程的存活观察更新；RTB-019/T85明确从受信账本边界注入已结算费用观察，检查真实Pi的Packet/提议/最终派发。该安全夹具不执行或认证P4在线Advisor，配置仍off，记录中标注了合成观察来源；不得从它推断在线Advisor已实现。线上调用计量属于对应运行适配和阶段验收。

完整运行保留前述全部失败。最后一次小范围失败是测试把带id的SQLite行整体与三字段普通对象比较，已改为独立字段断言；没有放宽资金/资源含义。类型、静态计划及严格OpenSpec校验仍通过。

下一步进入W3的恢复/生命周期/原崩溃矩阵；W1全量领域oracle审计和W4–W7继续按原清单。Qwen实网profile与请求上限仍待输入，原五项大型验收任务仍开放。

## W2剩余义务复核后的检查点

当前完整运行`2026-09-09T19-29-04-757Z`：772通过、0失败/取消/跳过，16,776断言；源码/测试摘要`086fc1341e62a9bcf8a69fd67074e3fee43cc6e233b118eb250086b16f312184`。最低缺口119；矩阵缺口313。原W2的15项最低缺口现均有证据，详细变体审核不自动关闭。

本阶段在已有真实proposal-valid/stale场景中补齐T59/T60/T62–T65/T70/RTB-019/T85的具体输入、断言与下一次父模型输入；只新增目录身份U和过期pending恢复S两条回归，没有复制整套夹具。相对初始746项，累计新增21条运行测试，验收分母仍为707/436。

新增发现并修复：CLI非法JSON及越权提议现在留下受控拒绝索引；它们不改变语义revision，也不造成默认advance。过期pending若不移除会永久阻挡后续resume，原反例失败后已修复为保留拒绝记录并退出pending。候选变化后，未成功运行的baseline也会重新准入，真实Pi最终能完成原目标，而非只停在拒绝状态。

T60使用同state root真实资源进程的存活观察更新；RTB-019/T85明确从受信账本边界注入已结算费用观察，检查真实Pi的Packet/提议/最终派发。该安全夹具不执行或认证P4在线Advisor，配置仍off，记录中标注了合成观察来源；不得从它推断在线Advisor已实现。线上调用计量属于对应运行适配和阶段验收。

完整运行保留前述全部失败。最后一次小范围失败是测试把带id的SQLite行整体与三字段普通对象比较，已改为独立字段断言；没有放宽资金/资源含义。类型、静态计划及严格OpenSpec校验仍通过。

下一步进入W3的恢复/生命周期/原崩溃矩阵；W1全量领域oracle审计和W4–W7继续按原清单。Qwen实网profile与请求上限仍待输入，原五项大型验收任务仍开放。

## W3第一批：生命周期与实际进程边界

当前完整运行`2026-09-09T19-29-04-757Z`：772通过、0失败/取消/跳过，16,776断言；源码/测试摘要`086fc1341e62a9bcf8a69fd67074e3fee43cc6e233b118eb250086b16f312184`，已精确匹配。类型检查通过。最低缺口119；细分矩阵123/436，仍缺313。

复用RPC/PTY夹具新增5个实际宿主场景：new session、switch session、fork、SIGTERM关闭、PTY /reload；扩展已有终端协议回应、重启、deadline场景的对应观察。没有新平台、协议或模型绑定，也没有新增矩阵key。

- 生命周期变化后，旧恢复scope保持PAUSED、旧owner失效、原history/notBefore保留；跨过旧冷却时刻仍没有恢复发送。新context不继承原故障/intent；它可以合法为IDLE或PAUSED，不强制某个内部初始状态。
- 真实PTY协议回应不被当作人工输入；继续使用原scope成功恢复。PTY /reload确实完成，并保留原限流历史/暂停状态。
- 原restart-wait现在记录两个不同Pi PID、原进程SIGKILL、同session/scope/history和恢复请求的墙钟时刻。原wait-deadline记录独立Pi进程、owner-process身份，跨过原notBefore仍不发请求，显式resume也不刷新deadline。
- 首轮测试误把新上下文约束为IDLE、把BLOCKED强制等同于owner已撤销，并混用了performance.now与墙钟；已按实际契约修正测试观察，不改运行时来迎合这些假设。失败工件保留。

新增最低证据是REC-020:A/E、T37:A/E、REC-009:P、REC-011:P，共6项；不是完成全部生命周期/await/crash矩阵。当前缺口按主工作包为W1 35、W2 0、W3 30、W4 25、W5 23、W6 6；所有详细review/closure仍按原退出条件。

相对初始746项，本次累计增加26条运行回归（W2 21、W3第一批5）；原707/436分母和历史报告未改写。尚未部署、归档或写真实Pi/Claude home，实网Qwen仍等待profile引用及总请求上限。

## W2剩余义务复核后的检查点

当前完整运行`2026-09-09T20-06-39-144Z`：777通过、0失败/取消/跳过，16,845断言；源码/测试摘要`925b21901eb27b7ab6bc6dff8e1b5b7eae7d3454606598c9f2d617ce0b2a7f7f`。最低缺口114；矩阵缺口313。原W2的15项最低缺口现均有证据，详细变体审核不自动关闭。

本阶段在已有真实proposal-valid/stale场景中补齐T59/T60/T62–T65/T70/RTB-019/T85的具体输入、断言与下一次父模型输入；只新增目录身份U和过期pending恢复S两条回归，没有复制整套夹具。相对初始746项，累计新增21条运行测试，验收分母仍为707/436。

新增发现并修复：CLI非法JSON及越权提议现在留下受控拒绝索引；它们不改变语义revision，也不造成默认advance。过期pending若不移除会永久阻挡后续resume，原反例失败后已修复为保留拒绝记录并退出pending。候选变化后，未成功运行的baseline也会重新准入，真实Pi最终能完成原目标，而非只停在拒绝状态。

T60使用同state root真实资源进程的存活观察更新；RTB-019/T85明确从受信账本边界注入已结算费用观察，检查真实Pi的Packet/提议/最终派发。该安全夹具不执行或认证P4在线Advisor，配置仍off，记录中标注了合成观察来源；不得从它推断在线Advisor已实现。线上调用计量属于对应运行适配和阶段验收。

完整运行保留前述全部失败。最后一次小范围失败是测试把带id的SQLite行整体与三字段普通对象比较，已改为独立字段断言；没有放宽资金/资源含义。类型、静态计划及严格OpenSpec校验仍通过。

下一步进入W3的恢复/生命周期/原崩溃矩阵；W1全量领域oracle审计和W4–W7继续按原清单。Qwen实网profile与请求上限仍待输入，原五项大型验收任务仍开放。

## W3第二批：ACK丢失后的对账与后续进展

当前完整运行`2026-09-09T20-06-39-144Z`：777通过、0失败/取消/跳过，16,845断言；源码/测试摘要`925b21901eb27b7ab6bc6dff8e1b5b7eae7d3454606598c9f2d617ce0b2a7f7f`，精确匹配。类型检查通过。最低缺口114；细分矩阵仍123/436、缺313。

状态反例先复现：执行已确认完成，ACK promise仍不返回，旧tick使下一次额度事故不能开始。修复按完整intent/scope/epoch/leaf元组查找唯一持久native entry，补记nativeId；只有确认终止才结算并结束ACK listener。匹配缺失、冲突或终止未知仍保持unknown与占用。

新增5条针对该缺陷的运行回归：1条状态卡住反例、2条身份不可用反例、1条纯身份规则检查、1条真实Pi场景。真实夹具只截断ACK交付；原入队、模型HTTP、文件工具、持久消息都保留。第一次恢复只写一次，第二个用户目标限流后也可继续；两个原生身份分别对账到原intent。没有以同进程假调用充当模型接收端，也没有借用该场景为C0–C5操作系统崩溃矩阵加分。

独立负向控制只移除终态后的ACK等待清理：真实Pi停在4个请求，第二次恢复无法进展，按预期失败；源码、manifest和失败现场保留于test-results/ack-wait-negative-control/。当前777项通过使用正常代码。

新增最低证据是REC-021:A/P/E、T40:P/E，共5项。相对本轮初始746项，累计新增31条运行回归；原707义务和436矩阵未扩展。当前主工作包最低缺口为W1 35、W2 0、W3 25、W4 25、W5 23、W6 6。完整domain/observer审计与所有矩阵仍须按原清单收口，releaseReady=false。

## W3第三批：TK11重启观察补齐，动态运行受环境阻塞

本批只修改tests/pi-rpc.test.ts：扩展原restart-wait，并新增restart-overdue，观察父进程离线跨过冷却时刻仍无请求/intent、原记录保留，以及同session重启后唯一continuation和原任务完成。生产代码未变，707/436分母未增加。设计、判定与固定续跑命令见[w3-tk11-restart-2026-09-10.md](w3-tk11-restart-2026-09-10.md)。

只读Node权限下的类型检查、静态计划检查和OpenSpec严格校验通过。当前执行环境拒绝bwrap创建NETLINK_ROUTE socket；两次隔离启动均未执行测试。未关闭隔离，未写真实Pi/Claude home，也未计入新证据。最新777通过/114最低缺口/313矩阵缺口仍为旧检查点；runtime-progress.exactCurrentProductSource=false，afterCheckpointWork记录本批待验证改动。TK11 A/E仍未关闭，历史任务71/76不变。

## 用户授权Pi接手后的收口记录

已在OpenSpec创建pi-execution-handoff.md，并从tasks.md、closure-plan.md及implementation-progress.md链接。它逐项保留原114个最低缺口、313个矩阵缺口，以及已获最低证据但仍需完整oracle审查的593/123项；没有新增验收分母。明确区分单元判定已通过、RPC代码已补未运行、规则接口/实际observer仍需实施、Q3执行/认证汇入路径与环境输入待完成。

本轮修复coverageReport对非法assertions/status的校验，反例先失败、修复后通过；零断言仍保持missing，未放宽发布门槛。新增10条单元测试覆盖12个原U义务的判定或已定位报告缺陷，分布在既有测试文件与4个聚焦新文件。最终12个文件40项只读检查通过、0失败/取消/跳过；以Node permission仅授予包目录读权限，不启动Pi或子进程，不授予文件写入。I/O故障用显式单元替身，不冒充真实数据库/磁盘故障。日志、失败反例、最终候选源快照和逐文件清单在test-results/readonly-2026-09-10/。

动态隔离仍被NETLINK_ROUTE权限拒绝；没有再次重复尝试同一环境阻塞。最后777完整通过报告和114/313缺口保持历史值，runtime-progress.exactCurrentProductSource=false。新实现和单元结果待Pi完成对应集成回归及完整报告后才能回填发布证据。五个历史大型任务仍开放，Qwen路线/profile和请求上限仍未提供。
