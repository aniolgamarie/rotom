# 2026-09-09 实现与验收增量

本轮完整运行 `2026-09-09T06-43-18-609Z`：727 passed，0 failed/cancelled/skipped，14740 条记录断言。报告为 [report.json](../../test-results/2026-09-09T06-43-18-609Z/report.json)。当前产品/测试源码摘要与报告一致；类型检查、OpenSpec strict、设计检查及14个设计负向控制均通过。

| 指标 | 本轮开始 | 本轮结束 |
|---|---:|---:|
| 完整通过测试 | 689 | 727 |
| P0–P3 最低场景/故障层级缺口 | 210 | 171 |
| 展开矩阵已获证据 | 117/436 | 123/436 |
| OpenSpec 大项 | 71/76 | 71/76 |

新增38项测试，补齐39项最低证据义务。剩余 U18、S0、A32、P39、E73、V7、L2；S0仅指按场景/故障ID登记的最低层级证据，不能当作细分时序/领域配方全部完成。展开矩阵另有313项缺口，与171不能相加。releaseReady=false。

## 本轮变更与独立观测

- 恢复准入、原生流终态、精确owner身份和intent首次派发成为生产调用的纯规则；补单条件反例与合法正例。九个守卫移除变异均被实际测试检出，恢复后通过。
- 修复定向测试过滤参数位于文件名后被Node忽略的问题。旧入口实际执行未选中的失败测试；修复后前置/后置/等号形式过滤及越界/缺文件/空筛选检查通过。
- 补齐continuation.C0–C5状态切点，保留原action/incident及账本；补迁移中断、intent/预算丢失、owner失联和最后资源槽状态测试。
- 真实Pi基线构建环境失败沿verifier→账本/回执→命令/工具→下一模型输入传播，不启动writer、critic或升级。
- 十个实际Pi父会话共享一个quota恢复许可，分别验证逐一完成、持有者取消、持有者被杀死但终态未落盘。实际接收器、原生事件、独立DB和/throttle status交叉核对。容量10的私有变异触发20次接收而非11，测试失败；恢复后通过。
- 错误全文脱敏仍保留失败类别、实际路线与验收缺口；两个状态目录的doctor明确独立协调；未认证SOCKS路径不静默直连；选择器跨状态尝试timer/spawn/write被检出。

状态模拟和实际Pi证据分别标为S与A/P/E；loopback未冒充Qwen实网认证。配置/Provider/模型/账号/网络仍是参数。v6设计、设计哲学、来源索引与原故障矩阵和用户设计包逐字节一致。

## 尚未完成

7.4实网Qwen长任务验收、10.2完整真实请求路径认证、11.6参数化策略效果对照、12.1完整场景/变体/observer审核、13.5完整Q1/Q2/Q3与await竞态仍保留未完成。其中169项最低证据缺口为非live层级，仍需要实现或补测；2项live义务待明确绑定及请求上限。未部署、同步真实home、提交或归档。

详细技术依据见[决策日志I66–I71](../decisions.md)，当前清单见[runtime-gap-cases.csv](runtime-gap-cases.csv)。失败的开发夹具及隔离变异结果保留在各自运行目录，未标为完整通过的报告。
