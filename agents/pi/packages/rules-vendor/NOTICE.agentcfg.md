# agentcfg adaptation

Upstream @tigorhutasuhut/pi-rules 0.6.0, MIT. Original compiled sources and declarations retained. Rule discovery uses explicit manifest root references through the supervisor, with no HOME/project fallback. Unknown frontmatter and invalid selected rules fail. Runtime events cannot expand sources; automatic nudges require the ordinary-helper gate. dist source maps describe the upstream build, not this adaptation.
