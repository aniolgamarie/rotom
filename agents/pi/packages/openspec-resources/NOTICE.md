# OpenSpec 1.11.0 Pi resources

Skills are generated without instruction changes from the integrity-verified upstream 1.11.0 templates. Full MIT license and generation sources are retained. The wrapper pins the upstream CLI as an exact dependency; it does not install or execute OpenSpec on resource discovery. SOURCE.json records the original tarball and generated file hashes.
