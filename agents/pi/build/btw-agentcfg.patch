--- a/dist/index.ts
+++ b/dist/index.ts
@@ -1,3 +1,5 @@
+import { pluginSettings } from "@agentcfg/pi-runtime/plugin-settings";
+import { pluginModelMethod } from "@agentcfg/pi-runtime/plugin-model";
 // @generated by scripts/build-runtime.mjs; do not edit.
 // @ts-nocheck -- generated JavaScript uses a .ts extension for Pi's Jiti loader.
 
@@ -1055,22 +1057,16 @@
 function effectiveRememberThinkingLevelChanges(settings) {
   return settings.rememberThinkingLevelChanges ?? DEFAULT_REMEMBER_THINKING_LEVEL_CHANGES;
 }
-async function readBtwSettings(settingsPath = btwSettingsPath()) {
-  await awaitBtwSettingsWrites(settingsPath);
-  return readBtwSettingsUncoordinated(settingsPath);
-}
-function updateBtwSettings(patch, options = {}) {
-  const settingsPath = options.settingsPath ?? btwSettingsPath();
-  return enqueueMutation(settingsPath, async () => {
-    options.signal?.throwIfAborted();
-    const current = await readSettingsDocumentForUpdate(settingsPath);
-    const updated = applyBtwSettingsPatch(current, patch);
-    const settings = normalizeBtwSettings(updated);
-    if (!settings) throw invalidSettingsError(settingsPath, "invalid settings shape");
-    await publishSettings(settingsPath, updated, options.signal, options.beforeRename);
-    return settings;
-  });
-}
+async function readBtwSettings(_settingsPath = btwSettingsPath()) {
+  const value = pluginSettings("pi-btw", "btw");
+  if (Object.keys(value).some(key => !["model", "thinkingLevel", "rememberThinkingLevelChanges"].includes(key))) throw new Error("AGENTCFG_BTW_CONFIG");
+  const settings = normalizeBtwSettings(value);
+  if (!settings) throw new Error("AGENTCFG_BTW_CONFIG");
+  return { kind: "loaded", settings };
+}
+
+function updateBtwSettings(_patch, _options = {}) { return Promise.reject(new Error("此设置由 agentcfg 管理；请更新机器覆盖并重新部署。")); }
+
 async function awaitBtwSettingsWrites(settingsPath = btwSettingsPath()) {
   await mutationQueues.get(settingsPath);
 }
@@ -2334,13 +2330,10 @@
 
 // src/btw.ts
 var MAX_CONTEXT_CHARS = 4e4;
-function createModelRegistryCompleteSimple(modelRegistry) {
-  return async (model, context, options) => {
-    const provider = modelRegistry.getProvider(model.provider);
-    if (!provider) throw new Error(`No provider registered for model provider: ${model.provider}`);
-    return provider.streamSimple(model, context, options).result();
-  };
-}
+function createModelRegistryCompleteSimple(_modelRegistry) {
+  return pluginModelMethod("pi-btw", "completeSimple");
+}
+
 async function resolveBtwModel({
   settings,
   currentModel,
--- a/package.json
+++ b/package.json
@@ -1,57 +1,58 @@
 {
-	"name": "@narumitw/pi-btw",
-	"version": "0.55.3",
-	"description": "Pi extension that adds a /btw side-question command.",
-	"type": "module",
-	"license": "MIT",
-	"private": false,
-	"keywords": [
-		"pi-package",
-		"pi-extension",
-		"pi",
-		"btw",
-		"side-question"
-	],
-	"files": [
-		"src",
-		"dist",
-		"README.md",
-		"LICENSE"
-	],
-	"pi": {
-		"extensions": [
-			"./dist/index.ts"
-		]
-	},
-	"piExtension": {
-		"lifecycle": "stable"
-	},
-	"scripts": {
-		"build": "node scripts/build-runtime.mjs",
-		"check": "npm run build && biome check . && npm run typecheck",
-		"format": "biome check --write .",
-		"typecheck": "tsc --noEmit",
-		"prepack": "npm run build"
-	},
-	"devDependencies": {
-		"@biomejs/biome": "2.5.10",
-		"@earendil-works/pi-ai": "0.84.3",
-		"@earendil-works/pi-coding-agent": "0.84.3",
-		"@earendil-works/pi-tui": "0.84.3",
-		"esbuild": "0.28.2",
-		"typescript": "7.0.2"
-	},
-	"repository": {
-		"type": "git",
-		"url": "https://github.com/narumiruna/pi-extensions",
-		"directory": "packages/pi-btw"
-	},
-	"dependencies": {
-		"@narumitw/pi-tui-kit": "^0.58.1"
-	},
-	"peerDependencies": {
-		"@earendil-works/pi-ai": "*",
-		"@earendil-works/pi-coding-agent": "*",
-		"@earendil-works/pi-tui": "*"
-	}
+  "name": "@narumitw/pi-btw",
+  "version": "0.55.3-agentcfg.1",
+  "description": "Pi extension that adds a /btw side-question command.",
+  "type": "module",
+  "license": "MIT",
+  "private": false,
+  "keywords": [
+    "pi-package",
+    "pi-extension",
+    "pi",
+    "btw",
+    "side-question"
+  ],
+  "files": [
+    "src",
+    "dist",
+    "README.md",
+    "LICENSE"
+  ],
+  "pi": {
+    "extensions": [
+      "./src/index.ts"
+    ]
+  },
+  "piExtension": {
+    "lifecycle": "stable"
+  },
+  "scripts": {
+    "build": "node scripts/build-runtime.mjs",
+    "check": "npm run build && biome check . && npm run typecheck",
+    "format": "biome check --write .",
+    "typecheck": "tsc --noEmit",
+    "prepack": "npm run build"
+  },
+  "devDependencies": {
+    "@biomejs/biome": "2.5.10",
+    "@earendil-works/pi-ai": "0.84.3",
+    "@earendil-works/pi-coding-agent": "0.84.3",
+    "@earendil-works/pi-tui": "0.84.3",
+    "esbuild": "0.28.2",
+    "typescript": "7.0.2"
+  },
+  "repository": {
+    "type": "git",
+    "url": "https://github.com/narumiruna/pi-extensions",
+    "directory": "packages/pi-btw"
+  },
+  "dependencies": {
+    "@narumitw/pi-tui-kit": "^0.58.1",
+    "@agentcfg/pi-runtime": "1.0.0"
+  },
+  "peerDependencies": {
+    "@earendil-works/pi-ai": "*",
+    "@earendil-works/pi-coding-agent": "*",
+    "@earendil-works/pi-tui": "*"
+  }
 }
--- a/src/btw.ts
+++ b/src/btw.ts
@@ -1,3 +1,4 @@
+import { pluginModelMethod, selectedPluginModel, selectedPluginModels } from "@agentcfg/pi-runtime/plugin-model";
 import {
 	type Api,
 	clampThinkingLevel,
@@ -86,13 +87,9 @@
 type BtwProviderRegistry = Pick<ExtensionCommandContext["modelRegistry"], "getProvider">;
 
 export function createModelRegistryCompleteSimple(
-	modelRegistry: BtwProviderRegistry,
+  _modelRegistry: BtwProviderRegistry,
 ): CompleteSimpleFunction {
-	return async (model, context, options) => {
-		const provider = modelRegistry.getProvider(model.provider);
-		if (!provider) throw new Error(`No provider registered for model provider: ${model.provider}`);
-		return provider.streamSimple(model, context, options).result();
-	};
+  return pluginModelMethod("pi-btw", "completeSimple");
 }
 
 interface ResolveBtwModelOptions {
@@ -122,51 +119,13 @@
 	modelRegistry,
 	warn,
 }: ResolveBtwModelOptions): Promise<ResolvedBtwModel | undefined> {
-	const reportWarning = (message: string) => warn?.(sanitizeSingleLine(message));
-	if (settings.model) {
-		const fallback = currentModel
-			? `${currentModel.provider}/${currentModel.id}`
-			: "the current model";
-		const reference = parseBtwModelReference(settings.model);
-		if (!reference) {
-			reportWarning(`pi-btw model ${settings.model} is invalid; falling back to ${fallback}.`);
-			return resolveBtwModel({ settings: {}, currentModel, modelRegistry, warn: reportWarning });
-		}
-		const configuredModel = modelRegistry.find(reference.provider, reference.modelId);
-		if (!configuredModel) {
-			reportWarning(`pi-btw model ${settings.model} was not found; falling back to ${fallback}.`);
-		} else {
-			const sameAsCurrent =
-				configuredModel === currentModel ||
-				(configuredModel.provider === currentModel?.provider &&
-					configuredModel.id === currentModel.id);
-			const fallbackAction = sameAsCurrent
-				? "no distinct current model is available"
-				: `falling back to ${fallback}`;
-			try {
-				const auth = await modelRegistry.getApiKeyAndHeaders(configuredModel);
-				if (auth.ok && hasRequestAuth(auth)) return { model: configuredModel, auth };
-				const reason = auth.ok ? "has no request credentials" : auth.error;
-				reportWarning(
-					`pi-btw model ${settings.model} is unavailable (${reason}); ${fallbackAction}.`,
-				);
-			} catch (error: unknown) {
-				reportWarning(
-					`pi-btw model ${settings.model} credentials failed (${formatError(error)}); ${fallbackAction}.`,
-				);
-			}
-			if (sameAsCurrent) return undefined;
-		}
-	}
-
-	if (!currentModel) return undefined;
-	try {
-		const auth = await modelRegistry.getApiKeyAndHeaders(currentModel);
-		if (auth.ok && hasRequestAuth(auth)) return { model: currentModel, auth };
-	} catch {
-		// The caller reports the final lack of an available model.
-	}
-	return undefined;
+  const model = selectedPluginModel("pi-btw", settings.model, currentModel) as Model<Api>;
+  try {
+    const auth = await modelRegistry.getApiKeyAndHeaders(model);
+    if (auth.ok && hasRequestAuth(auth)) return { model, auth };
+  } catch { /* 原生认证错误可能带私密信息，只反馈固定状态。 */ }
+  warn?.("PI_BTW_AUTHENTICATION_REQUIRED");
+  return undefined;
 }
 
 function hasRequestAuth(auth: SideQuestionAuth): boolean {
@@ -357,7 +316,7 @@
 	resumeThreads: readonly BtwResumeThreadSummary[],
 ): Promise<BtwCommandMenuResult> {
 	const currentModel = ctx.model;
-	const availableModels = ctx.modelRegistry.getAll();
+	const availableModels = selectedPluginModels("pi-btw") as Model<Api>[];
 	const currentThinkingLevel = pi.getThinkingLevel();
 	const loaded = await readBtwSettings();
 	const settings = loaded.kind === "loaded" ? loaded.settings : {};
--- a/src/fullscreen-ui.ts
+++ b/src/fullscreen-ui.ts
@@ -1,6 +1,5 @@
-import { spawn } from "node:child_process";
+import { terminalHyperlink, terminalClipboard } from "@agentcfg/pi-runtime/service-bindings";
 import {
-	copyToClipboard as copyToHostClipboard,
 	type ExtensionCommandContext,
 	type KeybindingsManager,
 	type Theme,
@@ -70,8 +69,8 @@
 			createBtwFullscreenTui(
 				parent,
 				theme,
-				dependencies.openUrl ?? openUrlInBrowser,
-				dependencies.copyToClipboard ?? copyToHostClipboard,
+				dependencies.openUrl ?? openUrlInTerminal,
+				dependencies.copyToClipboard ?? copyToTerminalClipboard,
 			));
 	let liveEditorText = ctx.ui.getEditorText();
 	let restoreEditor = false;
@@ -190,17 +189,12 @@
 	});
 }
 
-// Pi does not export its browser opener, so mirror its shell-free launcher for this isolated TUI.
-function openUrlInBrowser(target: string): void {
-	const [command, args] =
-		process.platform === "darwin"
-			? ["open", [target]]
-			: process.platform === "win32"
-				? ["rundll32", ["url.dll,FileProtocolHandler", target]]
-				: ["xdg-open", [target]];
-	spawn(command, args, { stdio: "ignore", detached: true })
-		.on("error", () => {})
-		.unref();
+// 用户点击后由终端呈现链接／剪贴板操作，不启动脱离监督的桌面子进程。
+function openUrlInTerminal(target: string): void {
+  process.stdout.write(terminalHyperlink(target));
+}
+async function copyToTerminalClipboard(text: string): Promise<void> {
+  process.stdout.write(terminalClipboard(text));
 }
 
 class BtwFullscreenHost<T> implements Component {
--- a/src/settings.ts
+++ b/src/settings.ts
@@ -1,3 +1,4 @@
+import { pluginSettings } from "@agentcfg/pi-runtime/plugin-settings";
 import { randomUUID } from "node:crypto";
 import { constants } from "node:fs";
 import { mkdir, open, rename, rm, writeFile } from "node:fs/promises";
@@ -74,28 +75,15 @@
 	return settings.rememberThinkingLevelChanges ?? DEFAULT_REMEMBER_THINKING_LEVEL_CHANGES;
 }
 
-export async function readBtwSettings(
-	settingsPath = btwSettingsPath(),
-): Promise<BtwSettingsLoadResult> {
-	await awaitBtwSettingsWrites(settingsPath);
-	return readBtwSettingsUncoordinated(settingsPath);
-}
-
-export function updateBtwSettings(
-	patch: BtwSettingsPatch,
-	options: UpdateBtwSettingsOptions = {},
-): Promise<BtwSettings> {
-	const settingsPath = options.settingsPath ?? btwSettingsPath();
-	return enqueueMutation(settingsPath, async () => {
-		options.signal?.throwIfAborted();
-		const current = await readSettingsDocumentForUpdate(settingsPath);
-		const updated = applyBtwSettingsPatch(current, patch);
-		const settings = normalizeBtwSettings(updated);
-		if (!settings) throw invalidSettingsError(settingsPath, "invalid settings shape");
-		await publishSettings(settingsPath, updated, options.signal, options.beforeRename);
-		return settings;
-	});
-}
+export async function readBtwSettings(_settingsPath = btwSettingsPath()): Promise<BtwSettingsLoadResult> {
+  const value = pluginSettings("pi-btw", "btw");
+  if (Object.keys(value).some(key => !["model", "thinkingLevel", "rememberThinkingLevelChanges"].includes(key))) throw new Error("AGENTCFG_BTW_CONFIG");
+  const settings = normalizeBtwSettings(value);
+  if (!settings) throw new Error("AGENTCFG_BTW_CONFIG");
+  return { kind: "loaded", settings };
+}
+
+export function updateBtwSettings(_patch: BtwSettingsPatch, _options: UpdateBtwSettingsOptions = {}): Promise<BtwSettings> { return Promise.reject(new Error("此设置由 agentcfg 管理；请更新机器覆盖并重新部署。")); }
 
 export async function awaitBtwSettingsWrites(settingsPath = btwSettingsPath()): Promise<void> {
 	await mutationQueues.get(settingsPath);
