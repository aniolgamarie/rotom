schema_version = 1

[roles]
default = "default"
smol = "smol"
slow = "slow"
vision = "vision"
plan = "plan"
advisor = "advisor"
task = "task"

[mcp_transports]
stdio = "stdio"
streamable-http = "http"
