# 9d6a9270 Linux验收与归档复核

日期：2026-09-23。本次读取现有源码和归档，执行报告schema/语义校验及只读正式汇总；未重跑完整mock/native、未执行live、未更改任务勾选或清理文件。

## 已独立核对

- 当前锁：`9d6a927093f066c9428a5c193a636c4d73c5e64c946d24dc6a08185cf3f0dbf0`。
- 锁中recipe_digest与当前源码重算值匹配：`85fc15c3babf58e768e2b09c25f7eac031644cac220c60c16d093c87cbb9257b`。
- `pi-cold-9d6a9270/index.json`所列349个文件SHA全部匹配。
- 四份cold报告均为passed；每份恰有first与第二组空格路径两个目标，安装三步骤顺序完整且退出码均为0。
- 每个目标的case集合与当前生产`native_cases(profile)`一致；全部native报告经过生产`validate_run_report`，锁、runtime、source、profile、platform均与目标匹配。
- 八目标共同源码摘要：`8b6c81207aab0b370bac3e14c880bbf0d39e3799645f063d2155cfa9c5ca128f`。

| Profile | 每目标case组数 | 两目标状态 |
|---|---:|---|
| pi-default | 7 | passed / passed |
| pi-cursor | 6（含ReadSeek） | passed / passed |
| pi-codex | 7 | passed / passed |
| pi-managed | 5（生命周期组含11个scenario） | passed / passed |

合计为**50份case报告、110个scenario行**。聊天摘要中的“56份case报告”应更正；归档的实际场景集合和110行通过结果不受该计数笔误影响。

## 正式汇总复验

以归档的`pi-scope-9d6a9270-r1-r2.json`和归档根目录作为输入，调用生产`read_scope()`与`report()`重新计算：

- passed：81
- failed：0
- not-run：283
- stale：0
- not-selected：0
- release_approved：false

重算结果与归档`pi-report-9d6a9270-only.json`**整份JSON相同，input_digest亦相同**。
与原固定scope比较，capability/scenario/platform/levels/applicability集合保持一致。
全部四个平台的40项live均为not-run，其中Linux x86_64有10项live待验收。

归档mock原始输出记录：1861 pytest passed、7 subtests passed；325 Node passed、0 skipped、0 failed。本次核对了这些原始输出，没有把它表述为本次重新执行测试。

## 结论与边界

可以接受本候选Linux x86_64四配方双路径安装及既定合成服务原生场景通过的阶段结论。此前混候选、场景缺项和手工汇总不一致的问题，在本次核对的归档与正式汇总中未再出现。

这不构成全平台、真实账号或完整迁移的发布批准。其他三平台、真实live及其后的T112仍按任务约定待完成。macOS代码的实际编译、OWNER恢复协议和原生行为须由对应平台验证，本次Linux复核不能替代。

清理需单独确认。归档report-only自足不等于所有旧目录均已审计；旧运行根、失败卷和原始记录应按各自清单核验。历史PID不能直接作为停止依据，应先核对当前进程身份与归属。
