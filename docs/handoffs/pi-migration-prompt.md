# 发给本机 Pi 的接手 prompt

将下面代码块内的内容完整发送给本机 Pi：

```text
请接手并继续完成 rotom/agentcfg 的 Pi 迁移。工作目录：
/data/1/weixiaoxian.wxx/dev_tool/rotom

先读 AGENTS.md 和这份交接文档：
docs/handoffs/pi-migration-2026-09-21.md
再读 specs/001-unify-pi-capabilities/ 下的 tasks.md、spec.md、plan.md、quickstart.md，以及交接文档链接的当前候选和验收报告。以仓库实际状态为准；不要把这段prompt当作全部需求，也不要从头重做已完成工作。

我的决定已经确定：
1. 彻底由model-delegate替换codex-delegate，七个旧Codex协调角色收敛为统一入口和用途模板，保留完整技能与显式高级操作。
2. 保留官方Codex CLI和原生工具；系统/企业/组织配置或账号无法核验时拒绝执行。不要改回默认MCP工具桥或维护Codex分支。
3. Codex、Cursor、MCP、web、代理和终端全部选择实网验收。但Pi本机--local配置及专用测试项目尚未准备；保留已选待验收，不借用当前Pi会话账号或随意选择真实项目，不改成not-selected。
4. 当前没有Linux arm64、macOS arm64、macOS x86_64测试入口，保留未验证。继续能在本机完成的代码和验证，不伪造跨平台通过。
5. 没有需要我新决策的工作请自主推进，包括必要修复、隔离mock、已授权的锁解析/依赖安装及使用合成服务的native/cold验证。真实账号调用必须等明确实例/项目/服务绑定和匹配native前提具备。遵守你自己的实际执行权限，不绕过安全限制。

交接快照：
- tasks为104/112，剩余T098、T106—T112；另外要补一份锁稳定后开始的完整mock回归。
- 当前锁：484f07e9f16e070296b88ba24dcf7a5900f9d098ed1d5f43340c4f31b281dd26。
- 有效隔离回归：1782 Python + 7 subtests、325 Node通过。
- Linux x86_64的default/Cursor双路径冷重建通过；Codex/managed第一目标原生全部通过。Codex第二目标资产大小/摘要校验失败，managed第二目标下载Python依赖时TLS握手失败。
- T098确有平台验收器接线缺口，不只是缺机器；读交接文档第5节逐项处理，不能删除平台guard来假装支持。
- 大量实现尚未提交，保留所有既有修改与未跟踪文件。
- 旧/tmp重跑脚本、固定工具链目录和沙箱挂载别名已经不存在。按交接文档重建执行入口；核验Node v24.14.0、npm 11.19.1、Bun 1.4.0以及Python/uv/Git/bwrap前提。
- 先前Codex执行通道的自动权限审核超时与UID映射问题不等于功能回归，也不是要求你每一步都问我许可。正常用户环境仍须遵守目录所有权、进程保护和校验规则。

执行顺序：
1. 检查当前源码/锁、已有工作、运行中的验证、工具链和空间。核对证据，不清理未知租约或历史失败。
2. 核查T098和完整能力覆盖；需要改代码时集中改完并做定向回归，再冻结候选，避免边长时间验收边改源码/schema。
3. 源码不变时，先补完整mock，再仅重跑Codex和managed缺失的两路径冷重建；复用仍匹配的default/Cursor证据。交接文档提供了直接调用生产cold_rebuild接口的代码，不需要旧/tmp脚本。
4. 源码或锁变化时，更新来源/许可证/调用方核对、完整四配方锁和候选身份，保持DSH锁不变；重新取得受影响的默认与原生证据，不给旧报告改身份。
5. 归档原始报告与输出并核对SHA/身份/完整场景/终止确认；更新tasks、实施记录和平台状态。按证据契约登记scope并运行report-only/check-release。未满足全部required和selected_optional时保持不批准。

持续修复能自主解决的问题。保留资产摘要与TLS校验、旧escaped/unknown和所有失败报告；不放宽安全规则、不复用旧运行包冒充冷重建、不将mock或安装成功当原生通过。实际缺机器、真实配置或你的执行通道不可用时，把具体剩余条件落盘，再向我汇报；不要反复询问已经确定的路线和六项服务选择。

最终请报告：完成了哪些代码/任务、最终锁身份、实际测试及两路径结果、T098接线完成情况，以及仍需外部环境或我提供输入的最小清单。不要笼统声称迁移全部完成。
```
