# OMP 验收范围修订 — 2026-09-24

用户明确要求：“macOS 验收、真实登录、真实 usage 和指定模型调用；Linux arm64 也仍未验证……这些也记录为遗留，不在本 spec 的范围，本机器目前没有这样的环境。”

这是对 `002-manage-omp-config` 验收边界的显式修订。当前范围包括已实现的软件功能、隔离回归、文档与本机 Linux glibc x64 无账号真实 smoke；八类九行配置能力、profile/秘密/所有权保护、login 与 usage 入口的软件契约继续保留。

## 转出映射

| 遗留项 | 原任务 | 转出范围 | 当前状态 |
|---|---|---|---|
| OMP-F01 | T061 的未覆盖架构 | Linux arm64 真实宿主验收 | 未验证，待环境 |
| OMP-F02 | T062 | macOS x64/arm64 真实宿主验收 | 未验证，待环境 |
| OMP-F03 | T063 | 新受管 profile 真实登录 | 未验证，待账号环境及独立授权 |
| OMP-F04 | T064 | 原生/受管真实账号 usage | 未验证，待账号环境及独立授权 |
| OMP-F05 | T065 | 指定 provider/model 真实调用 | 未验证，待服务环境及独立授权 |

详见[独立遗留清单](../../docs/follow-ups/omp-platform-and-live-validation.md)。转出不是执行成功、平台不支持或取消未来验证意图；它们不再阻塞当前 spec，不计入当前未完成任务，也不计入测试通过数量。当前无需提供测试机、账号或调用预算；后续是否建立新 spec、何时执行另行安排。

## 当前完成判定

- 当前任务 **62/62 已完成**：T001–T061、T066；原计划 66 个编号保留，T062–T065 转为外部遗留映射，不伪造完成勾选。
- T061 以本机 Linux glibc x64 为验收范围；[九行真实证据](evidence/linux-smoke.md)已通过。SC-001 只在此平台判定原生生效，不能推出其他平台通过。
- 隔离全量 2151 项及 7 子测试通过；后续自检扩展更新的受影响回归 57 项通过。证据的执行时间与范围保持原记录。
- 跨环境与秘密哨兵、登录门控、usage 透传等软件验收保持隔离测试要求；真实账号行为属于上述独立遗留。
- 四平台资产锁定、平台选择实现与模拟测试保留，支持矩阵仍清楚标记未做实机验证的平台。

## 原转出任务描述（仅供追溯）

以下保留原计划内容，不是当前待执行任务或通过记录：

- **T062** 在明确授权且macOS相应架构环境可用后，按T061相同的临时验收仓库复制、`.venv`符号链接复用、已登记`omp-validation`公开假值和正式锁规则，复现路径/发现/包校验及九行真实宿主验收；严禁假二进制/假receipt，不登录、不查真实usage、不发模型请求，将每架构结果写入 `specs/002-manage-omp-config/evidence/macos-smoke.md`；Linux结果不能替代，未执行平台持续标记未验证。

- **T063** 在对应明确登录授权后，以新建受管profile执行 `run omp -- login openai-codex`，在 `specs/002-manage-omp-config/evidence/login.md` 记录脱敏身份/命令/退出码及旧profile哨兵；认证不复制、不写证据，未授权保持未执行。

- **T064** 在对应真实usage授权后，验证原生/受管模式与选中上下文的实际账号，用 `specs/002-manage-omp-config/evidence/usage-live.md` 记录窗口/缓存/不支持/部分失败和provider差异；不以未登录或无账号smoke代替账号证据，不要求不受支持provider成功。

- **T065** 在指定provider/model的真实调用授权后，使用已验证受管身份执行最小生成请求，将实际路线/模型及结果写入 `specs/002-manage-omp-config/evidence/model-live.md`；不记录secret，不推定其他服务/平台通过。
