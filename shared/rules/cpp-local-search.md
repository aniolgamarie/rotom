<!-- 来源：本仓库按 rotom 大型 C++ 仓库局部导航需求原创定制；无外部上游来源声明。 -->

先读用户指定文件或模块，再限定路径、文件类型和精确符号检索；没有路径时先看浅层布局再选候选模块。使用有界 read/rg；readSeek 实际可用时先 digest 目标文件，再按需 def/refs，编辑前刷新锚点。不把截断输出当完整证据，不在启动时做全文扫描或全仓库索引。

C++ 修改前读取相关声明、实现及最短调用链，核对对象所有权/生命周期、RAII、回调捕获、线程与锁顺序、原子内存序，以及事务提交点、失败回滚和重试幂等性；查相邻测试和实际构建条件。报告文件:行号与观察到的契约后再改，不凭符号名称臆测语义，不顺手跨模块重构。

局部检索确有语义缺口时，优先用已有编译上下文；需新索引时说明最小范围、资源成本与写入位置并取得授权，不自动启动 Serena/clangd、不自动全量构建。保留原生权限和安全防护，不跟随链接绕过读取边界。
